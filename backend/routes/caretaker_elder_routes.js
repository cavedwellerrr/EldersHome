// import express from "express";
// import { protectStaff } from "../../middleware/authStaffMiddleware.js"; // adjust path
// import { requireCaretaker } from "../../middleware/authStaffMiddleware.js";   // same file
// import { getMyAssignedElders, getMyElderById } from "../controllers/caretaker_elder_controller.js"; // adjust path

// const router = express.Router();

// // List elders assigned to the logged-in caretaker
// router.get("/my", protectStaff, requireCaretaker, getMyAssignedElders);

// // (Optional) Get one elder the caretaker has access to
// router.get("/:elderId", protectStaff, requireCaretaker, getMyElderById);

// export default router;
